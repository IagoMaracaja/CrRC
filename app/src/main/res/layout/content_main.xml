<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <!-- Linear dos botões -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:background="@drawable/title_shape"
        android:padding="10dp">

        <ImageView
            android:id="@+id/iv_ok"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/ic_ok_1" />

        <ImageView
            android:id="@+id/iv_block"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/ic_block_1" />

        <ImageView
            android:id="@+id/iv_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/ic_refresh_1" />


    </LinearLayout>

    <!-- Linear das cartas -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp">

        <GridView xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/grid_cards"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:columnWidth="50dip"
            android:gravity="center"
            android:horizontalSpacing="0dip"
            android:numColumns="4"
            android:verticalSpacing="5dip">

        </GridView>

    </LinearLayout>

    <!-- Linear das informações -->
    <include layout="@layout/average_cost_layout" />

    <include layout="@layout/fixed_cards_layout" />

</LinearLayout>